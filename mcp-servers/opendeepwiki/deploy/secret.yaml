apiVersion: v1
kind: Secret
metadata:
  name: opendeepwiki-secret
  namespace: mcp-servers
  labels:
    app.kubernetes.io/name: opendeepwiki
type: Opaque
stringData:
  CHAT_API_KEY: "REPLACE_ME"  # kubectl create secret generic opendeepwiki-secret \
 # --namespace=mcp-servers \
 # --from-literal=CHAT_API_KEY='sk-ant-your-actual-key'
