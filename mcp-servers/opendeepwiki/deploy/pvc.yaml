apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: opendeepwiki-data
  namespace: mcp-servers
  labels:
    app.kubernetes.io/name: opendeepwiki
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ebs-gp2
  resources:
    requests:
      storage: 30Gi
